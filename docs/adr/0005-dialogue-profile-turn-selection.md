# ADR-0005 — Dialogue profile and turn selection

Status: Accepted

Live dialogue is permanently bound to the profile/CODEX_HOME that created its thread. Account switch requires hard deletion of that dialogue. Model/reasoning may change only between turns when runtime-validated. Every turn captures immutable selection.
