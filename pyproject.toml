[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-control"
version = "0.1.0"
description = "Foundation contracts for an autonomous per-server Codex control plane"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
