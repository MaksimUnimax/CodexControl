# Configuration examples

Non-secret examples only. Live Telegram tokens, Codex credentials/cookies and private keys are forbidden.

`server.toml` is the complete executable V1 schema. The matching secret
authority is a separate root-owned `secrets.env` containing only the token
assignment; it is intentionally not provided here.
